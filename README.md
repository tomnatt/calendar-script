# Calendar script

Script to maintain entries in my Google calendar.

Designed to be run locally. Required Google OAuth credentials.

`bundle exec rake`

Create the Service Account
Ensure the Service Account has access to your calendar
